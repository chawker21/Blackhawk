version: '3'
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "9000:9000"
        environment:
            DB_HOST: db
            DB_PORT: 3306
            DB_DATABASE: chawker21
            DB_USERNAME: root
            DB_PASSWORD: Blackhawk213!
    db:
        image: mysql:5.7
        environment:
            MYSQL_ROOT_PASSWORD: Blackhawk213!
            MYSQL_DATABASE: chawker21
        volumes:
            - ./dump.sql:/docker-entrypoint-initdb.d/dump.sql